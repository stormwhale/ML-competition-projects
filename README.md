Different competition projects from Kaggle:

A) "Loan_payback_ML" is a competition attempts that utilized PyTorch and GPU-based XGBoost to optimize for prediction

B) "Bank_marketing_ML" is a project based on a portuguese bank marketing campaign dataset. The goal is to analyze and predict potential clients for banking subscriptions. 
